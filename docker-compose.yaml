version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: esgseed
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=esgseed
      - POSTGRES_PASSWORD=esgseed1234
      - POSTGRES_DB=esgseeddb
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    networks:
      - microservices-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U esgseed -d esgseeddb" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Config Server - 가장 먼저 실행
  config-server:
    build:
      context: ./server/config
      dockerfile: Dockerfile
    container_name: config-server
    ports:
      - "8888:8888"
    networks:
      - microservices-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8888/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Eureka Server - Config Server 이후 실행
  eureka-server:
    build:
      context: ./server/eureka
      dockerfile: Dockerfile
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - microservices-network
    depends_on:
      config-server:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8761/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      - SPRING_CLOUD_CONFIG_URI=http://config-server:8888

  # Discovery Service - Eureka Server 이후 실행
  discovery-service:
    build:
      context: ./server/discovery
      dockerfile: Dockerfile
    container_name: discovery-service
    ports:
      - "8762:8762"
    networks:
      - microservices-network
    depends_on:
      eureka-server:
        condition: service_healthy
    environment:
      - SPRING_CLOUD_CONFIG_URI=http://config-server:8888
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/

  # Common Service
  common-service:
    build:
      context: ./service/common
      dockerfile: Dockerfile
    container_name: common-service
    ports:
      - "8080:8080"
    networks:
      - microservices-network
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - SPRING_CLOUD_CONFIG_URI=http://config-server:8888
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - DB_HOST=postgres
      - DB_USER=esgseed
      - DB_PASSWORD=esgseed1234
      - DB_NAME=common_db

  # User Service
  user-service:
    build:
      context: ./service/user
      dockerfile: Dockerfile
    container_name: user-service
    ports:
      - "8081:8081"
    networks:
      - microservices-network
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - SPRING_CLOUD_CONFIG_URI=http://config-server:8888
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - DB_HOST=postgres
      - DB_USER=esgseed
      - DB_PASSWORD=esgseed1234
      - DB_NAME=user_db

  # Soccer Service
  soccer-service:
    build:
      context: ./service/soccer
      dockerfile: Dockerfile
    container_name: soccer-service
    ports:
      - "8082:8082"
    networks:
      - microservices-network
    depends_on:
      eureka-server:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - SPRING_CLOUD_CONFIG_URI=http://config-server:8888
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - DB_HOST=postgres
      - DB_USER=esgseed
      - DB_PASSWORD=esgseed1234
      - DB_NAME=esgseeddb

networks:
  microservices-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
